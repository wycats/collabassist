DATABASE_URL="sqlite:local.db"
CARD_GENERATOR_MODE="fake"
AI_GATEWAY_MODEL="anthropic/claude-sonnet-4.6"
# Optional for local Gateway mode if you are not using Vercel OIDC.
# AI_GATEWAY_API_KEY=""
# Vercel OIDC is pulled by `pnpm exec vercel env pull .env.local` after login/link.
